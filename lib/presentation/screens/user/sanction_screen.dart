import 'package:flutter/material.dart';

class SanctionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Sanctions')),
      body: Center(child: Text('View your sanctions here.')),
    );
  }
}
